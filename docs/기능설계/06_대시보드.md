# 2-1. 대시보드 홈화면 개선

- **난이도**: 중 | **효과**: 최상
- **참고**: ERPNext Dashboard, Appsmith

## 수정 파일
| 파일 | 작업 |
|------|------|
| `server.js` | GET /api/dashboard 엔드포인트 추가 |
| `public/index.html` | 홈 탭 상단에 통계 카드 + 최근활동 타임라인 추가 |

## server.js — /api/dashboard
```js
app.get('/api/dashboard', requireAuth, (req, res) => {
  const categories = db.sql ? db.sql.categories.getAll() : [];
  const vendors = db.sql ? db.sql.vendors.getAll() : [];
  const quotes = db.sql ? db.sql.quotes.getAll() : [];
  const approvals = db['결재관리'].load().approvals || [];
  const thisMonth = new Date().toISOString().slice(0, 7);
  const monthQuotes = quotes.filter(q => (q.createdAt || '').startsWith(thisMonth));
  const monthTotal = monthQuotes.reduce((s, q) => s + (q.totalAmount || 0), 0);
  const pendingApprovals = approvals.filter(a => a.status === 'pending');
  const recentLogs = db['활동로그'] ? db['활동로그'].load().logs.slice(-10).reverse() : [];

  res.json({
    stats: {
      품목수: categories.length, 업체수: vendors.length,
      총견적수: quotes.length, 이번달견적: monthQuotes.length,
      이번달매출: monthTotal, 미처리결재: pendingApprovals.length,
    },
    recentActivity: recentLogs,
    pendingApprovals: pendingApprovals.slice(0, 5)
  });
});
```

## 프론트 — 통계 카드 (index.html 홈 탭)
```html
<div style="display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:16px;">
  <div class="card border-l-4 border-blue-500">
    <div class="text-sm text-gray-500">이번달 견적</div>
    <div class="text-2xl font-bold" x-text="dashboard.stats?.이번달견적 + '건'"></div>
    <div class="text-sm text-green-600" x-text="'₩' + (dashboard.stats?.이번달매출||0).toLocaleString()"></div>
  </div>
  <div class="card border-l-4 border-yellow-500">
    <div class="text-sm text-gray-500">미처리 결재</div>
    <div class="text-2xl font-bold" x-text="dashboard.stats?.미처리결재 + '건'"></div>
  </div>
  <div class="card border-l-4 border-green-500">
    <div class="text-sm text-gray-500">등록 품목</div>
    <div class="text-2xl font-bold" x-text="dashboard.stats?.품목수 + '종'"></div>
  </div>
  <div class="card border-l-4 border-purple-500">
    <div class="text-sm text-gray-500">거래 업체</div>
    <div class="text-2xl font-bold" x-text="dashboard.stats?.업체수 + '개'"></div>
  </div>
</div>

<!-- 최근 활동 타임라인 -->
<div class="card">
  <h3 class="font-bold mb-3">최근 활동</h3>
  <template x-for="log in dashboard.recentActivity || []">
    <div class="flex gap-3 py-2 border-b text-sm">
      <span class="text-gray-400" x-text="new Date(log.시간).toLocaleString('ko')"></span>
      <span class="font-medium" x-text="log.사용자"></span>
      <span x-text="log.행동"></span>
      <span class="text-blue-600" x-text="log.대상"></span>
    </div>
  </template>
</div>
```

## index.html function app() 추가 state/method
```js
dashboard: {},
async loadDashboard() {
  const res = await fetch('/api/dashboard');
  this.dashboard = await res.json();
},
// init() 내부에서 loadDashboard() 호출
```
